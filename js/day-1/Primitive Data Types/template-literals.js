const myName = "Avi";
const myAge = 37;
const aGreeting = `My name is ${myName} and I am ${myAge} Years old`;
console.log(aGreeting);

const length = 7;
const width = 3;
const area = length * width;
console.log(`The area of a rectangle with length of ${length} and a width of ${width} is ${area}`);

const today = "Sunday";
const taskOne = "Study";
const taskTwo = "Clean";
const taskThree = "Train";
console.log(`On ${today}, I will ${taskOne}, ${taskTwo} and ${taskThree}.`)

const numOfPets = 2;
console.log(`I have ${numOfPets} pets.`);

const base = 10;
const height = 5;
console.log(`The area of a triangle with base ${base} and height ${height} is ${(base * height) / 2}.`);